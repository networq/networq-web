{{ package.getFqpn() }}
====

This is the "{{ package.getFqpn}}" package for [NetworQ](https://github.com/networq).

{% if package.getTypes() %}
## Types

This package defines the following types that you can use in your own Networq:

{% for type in package.getTypes() %}
### {{ type.fqtn }} type

Fields:

{% for field in type.getFields() %}
  * `{{ field.getName() }}`: *{{ field.getType() }}*
{% endfor %}

{% endfor %}
{% endif %}
